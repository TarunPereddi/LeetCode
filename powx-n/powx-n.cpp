class Solution {
public:
Solution(){
        ios_base::sync_with_stdio(false);
        cin.tie(NULL);   
    }
    double myPow(double x, int n) {
        double X = pow(x,(double)n);
        return X;
    }
};